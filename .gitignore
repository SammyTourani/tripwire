# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/

# Secrets / credentials -- NEVER commit. The LiteLLM key lives only in a local
# untracked env file (see tripwire/runner/ and docs). Patterns are broad on purpose.
.env
.env.*
*.env
secrets/
*secret*
*.key
.litellm*

# Benchmark / run artifacts (regenerated; not source of truth)
runs/

# The COMPILOT paper (arXiv:2511.00592) -- kept LOCAL only. It carries an IEEE
# copyright notice; arXiv v2 is freely available, so it is not redistributed via
# this repo. Add it locally per docs/ADD_THE_PAPER.md as docs/compilot-paper.pdf.
docs/compilot-paper.pdf

# OS
.DS_Store
