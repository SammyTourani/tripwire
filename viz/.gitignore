# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# package-lock.json is regenerated by CI against public npm; not tracked
# because the maintainer's local npm is pinned to a private mirror that
# rewrites `resolved:` URLs. See .github/workflows/pages.yml.
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
